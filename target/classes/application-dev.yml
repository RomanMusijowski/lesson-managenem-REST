spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_dev?serverTimezone=UTC
    username: sfg_dev_user
    password: devUser@1234
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
    properties:
      javax:
        persistence:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: pgs_lesson_create.sql
